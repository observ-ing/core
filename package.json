{
  "name": "biosky",
  "version": "0.1.0",
  "private": true,
  "description": "Decentralized biodiversity platform on AT Protocol",
  "license": "AGPL-3.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w biosky-appview -w biosky-frontend",
    "dev": "npm run dev --workspace=biosky-appview",
    "generate-types": "node scripts/generate-types.js",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "appview": "npm run start --workspace=biosky-appview",
    "frontend:dev": "npm run dev --workspace=biosky-frontend",
    "frontend:build": "npm run build --workspace=biosky-frontend"
  },
  "devDependencies": {
    "@atproto/lex-cli": "^0.9.8",
    "@types/node": "^20.10.0",
    "@vitest/coverage-v8": "^4.0.17",
    "dotenv": "^17.2.3",
    "prisma": "^7.2.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "vitest": "^4.0.17"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "@prisma/client": "^7.3.0"
  }
}
