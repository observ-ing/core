{
  "name": "observing",
  "version": "0.1.0",
  "private": true,
  "description": "Decentralized biodiversity platform on AT Protocol",
  "license": "AGPL-3.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w observing-lexicon -w observing-frontend",
    "generate-types": "node scripts/generate-types.js",
    "generate-rust-types": "./scripts/generate-rust-types.sh",
    "frontend:dev": "npm run dev --workspace=observing-frontend",
    "frontend:build": "npm run build --workspace=observing-frontend"
  },
  "devDependencies": {
    "@atproto/lex": "^0.0.14",
    "@types/node": "^20.10.0",
    "dotenv": "^17.2.3",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
