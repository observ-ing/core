/** Header bar used by detail-page skeleton loaders (avatar + back-button row). */
export const detailHeaderSx = {
  p: 1.5,
  borderBottom: 1,
  borderColor: "divider",
  display: "flex",
  alignItems: "center",
} as const;
