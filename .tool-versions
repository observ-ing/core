# Tool versions for asdf / mise users.
# Run `asdf install` or `mise install` to populate.
#
# Rust is intentionally NOT pinned here — rust-toolchain.toml is the
# source of truth, and rustup auto-switches to the channel it declares
# on any cargo invocation.
nodejs 24
golang 1.26
